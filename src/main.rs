#![allow(long_running_const_eval)]
#![allow(unused)]

mod board;
mod constants;
mod functions;
mod moves;
mod qol;

use board::*;
use constants::*;
use functions::*;
use moves::*;
use qol::*;

fn main() {
    let mut state = State::default();
}
